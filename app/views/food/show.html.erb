<section id="food-information">
	<div class='page-header'>
		<h2><%= @food.name %></h2>
	</div>
</section>

<% if user_signed_in? %>
	<% state = current_user.get_state %>
<% else %>
	<% state = "CO" %>
<% end %>

<h2><%= @food.season_status(state) %></h2>