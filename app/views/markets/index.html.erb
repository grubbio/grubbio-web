<section id="small-search">
    <%= render partial: 'markets/search' %>
</section>

<section id="main">
  <%= render partial: "markets/list" %>
</section>

<section id="markets-pagination" style="margin-top: 20px;">
  <%= will_paginate @markets %>
  <br><br>
  (<%= Market.count %> total)
</section>